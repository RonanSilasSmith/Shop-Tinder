import ProductSelect from "../components/ProductSelect"

const Home = () => {
    return (
        <div className="container">
            <ProductSelect/>
        </div>
    );
};

export default Home;
